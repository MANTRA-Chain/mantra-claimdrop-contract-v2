# =============================================================================
# RPC Configuration
# =============================================================================
# IMPORTANT: Use HTTPS endpoints only. HTTP is insecure.
# Validate RPC URLs before use (scripts check format automatically)

MANTRA_DUKONG_RPC_URL=https://evm.dukong.mantrachain.io
MANTRA_MAINNET_RPC_URL=https://evm.mantrachain.io

# =============================================================================
# Private Key Configuration
# =============================================================================
# CRITICAL SECURITY:
#   - Never commit this file with real keys
#   - Set file permissions: chmod 600 .env
#   - Verify .env is in .gitignore
#   - Never share or log private keys
#
# Generate new key: cast wallet new
# Export from MetaMask: Settings > Advanced > Export Private Key

PRIVATE_KEY=your_private_key_here
LEDGER_ADDRESS=your_ledger_address_here

# =============================================================================
# Gas Configuration (Optional)
# =============================================================================
# Scripts use dynamic gas price estimation by default
# Manual override (in wei):
# GAS_PRICE=50000000000

# Gas price bounds (in wei)
# GAS_PRICE_MIN=10000000000    # 10 gwei minimum
# GAS_PRICE_MAX=200000000000   # 200 gwei maximum

# =============================================================================
# Transaction Configuration (Optional)
# =============================================================================
# Confirmation timeout in seconds (default: 180)
# TX_CONFIRMATION_TIMEOUT=180

# Campaign start buffer in seconds (default: 30)
# CAMPAIGN_START_BUFFER=30

# =============================================================================
# Debug Mode (Optional)
# =============================================================================
# Enable verbose logging (default: 0)
# DEBUG=1

# Force strict security mode on testnet (default: off)
# MAINNET_STRICT=1

# Gas Reporter
REPORT_GAS=false
COINMARKETCAP_API_KEY=your_coinmarketcap_api_key_here
